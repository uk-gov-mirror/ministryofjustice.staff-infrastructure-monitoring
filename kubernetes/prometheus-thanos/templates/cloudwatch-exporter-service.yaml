apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-cloudwatch-exporter
spec:
  selector:
    app: cloudwatch-exporter
  ports:
    - name: http
      protocol: TCP
      port: 9106
      targetPort: 9106